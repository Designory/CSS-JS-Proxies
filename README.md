# CSS-JS-Proxies

##Example Use:
```

const proxyBuilder = require('')

proxyBuilder({
		directory: 'path-to-file-location',
		baseUrl: 'http://some-url.com/some-path-to-proxies',
		jsFilename: 'js-file-min.js',
		cssFilename: 'cs-file-min.css',
		callback: callbackFunction
	});
```